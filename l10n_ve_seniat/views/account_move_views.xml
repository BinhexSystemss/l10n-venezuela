<?xml version='1.0' encoding='UTF-8' ?>
<odoo>
    <record id="view_move_form" model="ir.ui.view">
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form" />
        <field name="arch" type="xml">

           <group id="header_left_group" position="inside">
                <field
                    name="marck_paper"
                    attrs="{'invisible': [ '|', ('state','!=','posted'),('move_type', 'in', ['entry'])]}"
                />
                <field
                    name="maq_fiscal_p"
                    attrs="{'invisible': [('move_type', 'in',  ['out_invoice', 'out_refund', 'out_receipt', 'entry'])]}"
                />
                <field
                    name="l10n_ve_control_number"
                    attrs="{'required': [('maq_fiscal_p','=', False),('move_type', 'in', ['in_invoice', 'in_refund', 'in_receipt'])],
                                'invisible': [('move_type', 'in', ['entry'])]}"
                />
            </group>

        </field>
    </record>

   <record id="view_account_invoice_filter" model="ir.ui.view">
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_account_invoice_filter" />
        <field name="arch" type="xml">
            <field name="name" position="after">
                <field name="l10n_ve_control_number" />
            </field>
        </field>
    </record>

    <record id="view_invoice_tree" model="ir.ui.view">
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_invoice_tree" />
        <field name="arch" type="xml">
            <field name="invoice_date" position="before">
                <field name="l10n_ve_control_number" />
            </field>
        </field>
    </record>

</odoo>
